# 在 default.conf 的 server 块外追加，或替换 listen 443 配置
# 需先 certbot certonly --webroot -w /var/www/certbot -d api.hanjianglab.com

server {
    listen 443 ssl http2;
    server_name api.hanjianglab.com;

    ssl_certificate     /etc/letsencrypt/live/api.hanjianglab.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.hanjianglab.com/privkey.pem;

    client_max_body_size 25m;

    location / {
        proxy_pass http://pdf_tra_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
    }
}
